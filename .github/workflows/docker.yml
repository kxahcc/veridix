name: publish-docker

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: pip install -r services/requirements.txt
      - run: python scripts/fetch_tool_binaries.py
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build intermediate dev image
        run: |
          docker build -t ghcr.io/kxahcc/veridix/veridix-tools:dev             -f deploy/container/veridix-tools/Dockerfile .

      - name: Build full image
        run: |
          docker build -t ghcr.io/kxahcc/veridix/veridix-tools:full             -f deploy/container/veridix-tools/Dockerfile.full             --build-arg BASE_IMAGE=ghcr.io/kxahcc/veridix/veridix-tools:dev .
          docker push ghcr.io/kxahcc/veridix/veridix-tools:full

      - name: Build code-lite image
        run: |
          docker build -t ghcr.io/kxahcc/veridix/veridix-tools:code-lite             -f deploy/container/veridix-tools/Dockerfile.code-lite .
          docker push ghcr.io/kxahcc/veridix/veridix-tools:code-lite
